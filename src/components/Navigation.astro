<nav id="navbar">
    <a href="/" class="nav-item link">Home</a>
    <a href="/uses" class="nav-item link">Uses</a>
    <a href="/blog" class="nav-item link">Thoughts</a>
    <a href="/reading" class="nav-item link">Reading</a>
</nav>

<style>
#navbar {
    text-align: center;
    position: relative;
    display: flex;
    gap: 10px;
    justify-content: center;
    margin: 80px 0;
  }
.nav-item {
    font-family: var(--body-font);
    font-size: 14px;
    padding: 8px 16px;
    text-decoration: none;
    color: var(--subtext);
    transition: color 0.2s ease-in-out;
}
.nav-item:hover{
    color: var(--white);
}
#navbar > a {
  text-decoration: none;
  color: white;
}
#navbar > a.link {
  text-decoration: none;
  color: white;
  position: relative;
  display: inline-block;
}
#navbar a.link::before {
  content: "";
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 0px;
  background-color: var(--grey-600);
  transition: height 0.2s ease;
}
#navbar a.link:hover::before {
  height: 100%;
  background-color: var(--grey-600);
  z-index: -1;
}
</style>